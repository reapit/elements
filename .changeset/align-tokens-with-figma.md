---
"@reapit/elements": patch
---

Changed: Update `gallery_viewer` and `tab` design tokens to match Figma. Adds a `video_overlay` fill token and adjusts caption and button colours for `gallery_viewer`; adds disabled tokens and updates hover and selected text colours for `tab`.
