---
'@reapit/elements': minor
---

Added: `replace-deprecated-button-group-layout-props` codemod to migrate `ButtonGroup` usage from the deprecated `autoFlow` and `justifyContent` props to the new `orientation` and `align` props.
