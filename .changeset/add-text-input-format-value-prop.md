---
'@reapit/elements': minor
---

Added: `formatValue` prop to `TextInput`. Accepts a `(value: string) => string` function that renders formatted text in an overlay while preserving the raw value in the underlying input.
