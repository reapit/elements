---
'@reapit/elements': minor
---

Deprecated: `SupportedProductId` from `@reapit/elements/core/app-switcher`, and `AppSwitcher.AppAvatar` and `AppSwitcher.ProductMenuItem` namespace properties. Import `SupportedProductId`, `AppAvatar`, and `AppSwitcherProductMenuItem` from `@reapit/elements/core/app-switcher/anz` instead. Run the `rewrite-anz-app-switcher-imports` codemod to migrate automatically.
