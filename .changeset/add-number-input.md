---
'@reapit/elements': minor
---

Added: `NumberInput` component. Displays locale-aware formatted numbers via an overlay whilst keeping the raw numeric value (a canonical numeric string) as the input value. Accepts `locale`, `formatOptions`, `inputMode`, `min`, and `max` props.
