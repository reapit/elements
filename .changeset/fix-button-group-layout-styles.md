---
'@reapit/elements': patch
---

Fixed: `ButtonGroup` with `orientation="horizontal"` and `align="stretch"` now correctly stretches buttons to equal height. Fixed: deprecated `autoFlow` and `justifyContent` props no longer override `orientation` and `align` when both are set simultaneously. Fixed: `ButtonGroup` with `orientation="vertical"` now defaults to `align-items: start`.
