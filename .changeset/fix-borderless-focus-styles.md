---
'@reapit/elements': patch
---

Fixed: borderless `TextInput` and `ComboboxButton` now use a transparent border instead of `border: none`, so focus styles render correctly and layout remains stable.
