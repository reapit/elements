---
'@reapit/elements': minor
---

Added: `DeterminateProgressBar` and `IndeterminateProgressBar` components. `DeterminateProgressBar` communicates a task's completion progress via its numeric `value` prop. `IndeterminateProgressBar` communicates that a task is in progress when its completion progress cannot be determined.
