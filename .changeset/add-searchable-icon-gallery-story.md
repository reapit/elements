---
'@reapit/elements': patch
---

Internal: Add a searchable `Icons/Gallery` story to Storybook. Each tile shows the icon name and a **Copy import** button that copies the icon's subpath import statement to the clipboard. Search matches against the icon's name or a small set of curated synonyms in `src/icons/docs/icon-synonyms.json`.
