---
'@reapit/elements': minor
---

Added: `Listbox` accepts a `role` prop (`"listbox"` | `"tree"`) enabling ARIA tree widget semantics, hierarchical keyboard navigation (Arrow keys expand/collapse groups), and correct `role="treeitem"` on options and group headers.
