---
'@reapit/elements': minor
---

Added: `Video` utility component. Renders a `<video>` element styled to fill its container with configurable `objectFit` behaviour. Shows a fallback UI when the video fails to load; the default fallback announces the error to screen readers via `aria-live="polite"`.
