---
'@reapit/elements': minor
---

Added: `focused-layout-content-full-bleed` codemod. Migrates `FocusedLayout.Content` to pass `isFullBleed` and nest a `MainContainer size="wide"`, adopting the future default padding and maximum width behaviour now.
