---
'@reapit/elements': minor
---

Changed: `GalleryViewerDialog.Content` no longer applies padding. Padding is now the responsibility of the layout components (`GalleryViewerCarouselLayout` and `GalleryViewerMediaListLayout`) placed inside `Content`.
