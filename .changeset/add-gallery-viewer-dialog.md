---
'@reapit/elements': minor
---

Added: `GalleryViewerDialog` component at `src/core/gallery-viewer/dialog`. Provides a responsive full-screen dialog for gallery content — inset with a semi-transparent backdrop on large screens (≥1440px), and full-viewport on smaller screens.
