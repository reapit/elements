---
'@reapit/elements': minor
---

Added: `Toast` component. A presentation-only notification supporting `error`, `info`, `neutral`, `success`, and `warning` variants with an optional animated timeout bar.
