---
'@reapit/elements': minor
---

Added: `accessory` prop to `AccordionSummary`, replacing `rightInfo` for displaying optional content alongside the accordion title. Updated Figma Code Connect to target the new Accordion header component.
