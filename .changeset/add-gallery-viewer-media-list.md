---
'@reapit/elements': minor
---

Added: `GalleryViewer.MediaList` compound component. Renders a vertical `<ul>` of media items. Exposes `GalleryViewer.MediaListItem` and `GalleryViewer.MediaItemCaption`.
