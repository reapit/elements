---
'@reapit/elements': minor
---

Added: `keepMounted` prop to `Accordion`. When `false`, children are unmounted when the accordion is closed, deferring rendering until first open. Defaults to `true` (existing behaviour unchanged).
