---
"@reapit/elements": minor
---

Added: `borderColour` prop to `Avatar`, accepting any `--colour-border-*` design token, for a ring border. This is useful when an avatar sits over a coloured or patterned background and needs to stand out.
