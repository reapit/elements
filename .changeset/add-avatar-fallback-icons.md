---
"@reapit/elements": minor
---

Added: `Avatar`, `AvatarButton`, and `AvatarAnchor` now render a fallback icon when no image or `children` is provided — `user-outline` for the `circle` shape, and `building` for the `square` shape.
