---
'@reapit/elements': minor
---

Added: `ProgressIndicator` component. It communicates a task's completion progress via its numeric `value` prop, or that a task is in progress when its completion progress cannot be determined, if `value` is omitted.
