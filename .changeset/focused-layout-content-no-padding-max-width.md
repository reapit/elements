---
'@reapit/elements': minor
---

Added: `isFullBleed` prop to `FocusedLayout.Content`, letting consumers remove its default max-width and padding together when nesting a `MainContainer`, which applies its own.
