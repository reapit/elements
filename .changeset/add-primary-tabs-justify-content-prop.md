---
"@reapit/elements": minor
---

Added: `justifyContent` prop to `PrimaryTabs`, supporting `start` (default, sizes each tab to its content) and `stretch` (grows tabs to equally fill the available width).
