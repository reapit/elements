---
'@reapit/elements': minor
---

Changed: Split the `Input` and `InputGroup` migration guide into a simple index (`README.md`) and a comprehensive reference (`MIGRATION_GUIDE.md`). The full guide is now embedded in the Storybook v5 migration docs with a collapsible expand/collapse section, enabling viewers to copy-paste the guide directly for agent-assisted migration.
