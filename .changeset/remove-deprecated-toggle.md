---
'@reapit/elements': major
---

Removed: `Toggle`, `ToggleRadio`, and related exports from `src/deprecated/toggle`. Run the `replace-deprecated-toggle` codemod to migrate `Toggle` to `Switch`, and the `replace-deprecated-toggle-radio` codemod to migrate `ToggleRadio` to `ChipSelect`.
