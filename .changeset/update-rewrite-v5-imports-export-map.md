---
"@reapit/elements": patch
---

Fixed: `rewrite-v5-imports` codemod now correctly maps `AvatarAnchor`, `AvatarButton`, `AvatarGroup`, and `DevicesIcon`, and no longer maps the removed `ElFormLayout`/`ElInputWrap` family of exports.
