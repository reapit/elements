---
"@reapit/elements": minor
---

Added: optional `aria-label` prop to `Avatar`. When provided, it renders as a tooltip shown on hover or focus, and `Avatar` becomes a focusable `button` so keyboard users can reveal it.
