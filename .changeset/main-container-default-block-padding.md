---
'@reapit/elements': major
---

Changed: `MainContainer` now includes block (top and bottom) padding by default. Previously, only inline padding was applied. To restore the previous behaviour, use the new `hasNoTopPadding` and `hasNoBottomPadding` props.
