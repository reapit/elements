---
'@reapit/elements': minor
---

Deprecated: the default padding and maximum width applied by `FocusedLayout.Content`. These will be removed in the next major version. Nest a `MainContainer` inside `FocusedLayout.Content` and pass `isFullBleed` to adopt the future default now.
