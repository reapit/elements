---
"@reapit/elements": minor
---

Added: `icon`, `badge`, and `aria-disabled` props to `PrimaryTab`. `icon` and `badge` render alongside the label, and `aria-disabled` makes the tab appear disabled and ignore click events, while remaining focusable.
