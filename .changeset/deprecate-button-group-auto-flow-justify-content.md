---
'@reapit/elements': minor
---

Deprecated: `autoFlow` and `justifyContent` props on `ButtonGroup`. Use `orientation` and `align` instead. Run the `replace-deprecated-button-group-layout-props` codemod to migrate automatically.
