---
'@reapit/elements': minor
---

Added: `Grid` utility component with a `Grid.Item` subcomponent. Exposes mainstream CSS grid container and item properties as React props, with polymorphic `as` support on both components. Prop-driven styles take precedence over any `className` styles.
