---
'@reapit/elements': minor
---

Added: `formatFileSize` utility to `src/utils/number-format`. Formats a byte count as a localised size string using decimal (SI) units — `1 kB` is `1,000` bytes, `1 MB` is `1,000,000` bytes — e.g. `3.6 MB` in `en-GB`.
