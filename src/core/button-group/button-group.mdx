import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs/blocks'
import { ButtonGroup } from './index'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'
import * as ButtonGroupStories from './button-group.stories'
import { GuidelinesImage } from '../../storybook/figma'

<Meta of={ButtonGroupStories} />

<GuidelinesImage name="ButtonGroup" />

Styled button group component with multiple flavours depending on use case.

Props available for the React version of the component.

## Default Usage

A button with no addtional props / classes applied.

<Canvas of={ButtonGroupStories.Default} />

<RenderHtmlMarkup of={ButtonGroupStories.Default} />

## With Primary Button

A button group with primary button.

<Canvas of={ButtonGroupStories.WithPrimaryButton} />

<RenderHtmlMarkup of={ButtonGroupStories.WithPrimaryButton} />

## With Icon Only Button

A button group with icon only button.

<Canvas of={ButtonGroupStories.WithIconOnlyButton} />

<RenderHtmlMarkup of={ButtonGroupStories.WithIconOnlyButton} />
