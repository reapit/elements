import { styled } from '@linaria/react'

export const ElFilterBarLeftContent = styled.div`
  @layer elements.main {
    display: flex;
    align-items: center;
    flex-flow: row nowrap;
    gap: var(--spacing-3);
  }
`
