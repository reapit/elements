import { styled } from '@linaria/react'

export const ElSplitButton = styled.div`
  @layer elements.main {
    display: inline-flex;
    align-items: start;
    height: min-content;
    width: min-content;
    isolation: isolate;
  }
`
