import { css } from "@linaria/core";

/**
 * @deprecated Empty class retained for backwards compatibility. The Google
 * Fonts `@import` it used to carry now lives in `src/styles/globals.css`
 * and loads unconditionally, so consumers no longer need to apply this
 * class to receive the font.
 */
export const elFont = css``;
