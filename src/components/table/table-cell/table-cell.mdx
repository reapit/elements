import { Meta, Canvas, Controls } from '@storybook/blocks'
import { RenderHtmlMarkup } from '../../../storybook/render-html-markup'
import * as TableCellStories from './table-cell.stories'

<Meta of={TableCellStories} />

# TableCell
  Typically TableCell are used as TableRow children, and its will have children `div` as the tabelcell cildren container

## Basic Usage

<Canvas of={TableCellStories.BasicUsage} />
<Controls />

<RenderHtmlMarkup of={TableCellStories.BasicUsage} />
