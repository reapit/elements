import { Meta, Canvas, Controls } from '@storybook/blocks'
import { RenderHtmlMarkup } from '../../../storybook/render-html-markup'
import * as TableHeadStories from './table-head.stories'

<Meta of={TableHeadStories} />

# TableHead
This story demonstrates how to use the `TableHead` component in a basic table setup. It shows the table header with 5 columns, including a checkbox, text, and image headers.

## Basic Usage
<Canvas of={TableHeadStories.BasicUsage} />

## Basic Display With Sticky Header
<Canvas of={TableHeadStories.BasicUsage} />
<Controls />


<RenderHtmlMarkup of={TableHeadStories.BasicUsage} />
