import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as NavSearchButtonStories from './nav-search-button.stories'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'

<Meta of={NavSearchButtonStories} />

# NavSearchButtonStories

This component renders a search button that will only be used in the app bar.

## Styles Only Usage

<Canvas of={NavSearchButtonStories.StylesOnlyUsage} />

<RenderHtmlMarkup of={NavSearchButtonStories.StylesOnlyUsage} />

## React Usage

in React usage, the present of `shortcut` is true by default, it can be controlled by utilizing `isShortcutVisible` props

<Controls of={NavSearchButtonStories.ReactUsage} />

<Canvas of={NavSearchButtonStories.ReactUsage} />

<RenderHtmlMarkup of={NavSearchButtonStories.ReactUsage} />
