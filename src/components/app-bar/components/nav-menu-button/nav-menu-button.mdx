import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as NavMenuButtonStories from './nav-menu-button.stories'
import { RenderHtmlMarkup } from '../../../../storybook/render-html-markup'

<Meta of={NavMenuButtonStories} />

# NavMenuButton

## Styles Only Usage

The NavMenuButton is toggles the visibility of a Menu containing a list of buttons and/or links.

The 'top' property allows the Y position of the Menu below the button to be changed from 36px so that it can match styling of a parent component

The menu can be aligned to the left or right of the button

<Canvas of={NavMenuButtonStories.StylesOnlyUsage} />

<RenderHtmlMarkup of={NavMenuButtonStories.StylesOnlyUsage} />

## React Shorthand

<Controls />

In the React Shorthand, you can compose your NavMenuButton by applying props as appropriate following the `NavMenuButton` interface:

<Canvas of={NavMenuButtonStories.ReactShorthand} />

<RenderHtmlMarkup of={NavMenuButtonStories.ReactShorthand} />
