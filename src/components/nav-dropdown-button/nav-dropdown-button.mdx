import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as NavDropdownButtonStories from './nav-menu-button.stories'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'

<Meta of={NavDropdownButtonStories} />

# NavDropdownButton

## Styles Only Usage

The NavDropdownButton can be used toggle visibility of the `Menu` component.

The menu alignment can be set by setting `data-alignment="left | right"` to the `Menu` component

<Canvas of={NavDropdownButtonStories.StylesOnlyUsage} />

<RenderHtmlMarkup of={NavDropdownButtonStories.StylesOnlyUsage} />

## React Usage

Here you can use the `isOpen` prop to toggle the `chevron` icon, but it is recommended to compose it with `Menu` component as example here

<Canvas of={NavDropdownButtonStories.ReactUsage} />

<RenderHtmlMarkup of={NavDropdownButtonStories.ReactUsage} />
