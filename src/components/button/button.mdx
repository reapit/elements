import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'
import * as ButtonStories from './button.stories'

<Meta of={ButtonStories} />

# Button

<Controls />

Styled button component with multiple flavours depending on use case is used to trigger an action or event.

## Default Usage

A button with no additional props / classes applied

<Canvas of={ButtonStories.Default} />

<RenderHtmlMarkup of={ButtonStories.Default} />

## Button Variants

Different variants are supported, to enable buttons to be used in different contexts. The variant maps directly to the helper classes for non React users.

`Buttons` only support `primary`, `secondary`, `tertiary`, `destructive` and `busy`, the `secondary` being the default.

<Canvas of={ButtonStories.ButtonVariants} />
<RenderHtmlMarkup of={ButtonStories.ButtonVariants} />

## Busy

<Canvas of={ButtonStories.ButtonBusy} />
<RenderHtmlMarkup of={ButtonStories.ButtonBusy} />

## Button with Icon

<Canvas of={ButtonStories.ButtonWithIcon} />
<RenderHtmlMarkup of={ButtonStories.ButtonWithIcon} />

## Button Size

If you wish to size up or down a button, you can use the `size` prop. Available sizes are `small`, `medium` and `large`

By default all of our buttons are sized to `medium`.

<Canvas of={ButtonStories.ButtonSize} />
<RenderHtmlMarkup of={ButtonStories.ButtonSize} />

## Button Disabled

<Canvas of={ButtonStories.ButtonDisabled} />
<RenderHtmlMarkup of={ButtonStories.ButtonDisabled} />

## Button With href

With href prop, link can be displayed as button. Anchor attributes such as `target` and `rel` are supported and can be passed as button props, provided that the `href` prop is also included.

<Canvas of={ButtonStories.ButtonWithHref} />
<RenderHtmlMarkup of={ButtonStories.ButtonWithHref} />

## Button With hasNoPadding

The `hasNoPadding` prop is used with the `tertiary` variant of the button to demonstrate how removing padding and resetting the height can create a more minimalistic, compact button style.
This is useful when you need a button with no extra spacing or fixed height, while maintaining the button's core functionality and appearance. The prop is designed exclusively for the tertiary variant and will not affect other button variant types.

<Canvas of={ButtonStories.ButtonWithHasNoPadding} />
<RenderHtmlMarkup of={ButtonStories.ButtonWithHasNoPadding} />

## Other attributes

All other standard HTML attributes for `<button>` are supported and are passed through to the React component, e.g. type and onClick

<Canvas of={ButtonStories.StandardAttributes} />

<RenderHtmlMarkup of={ButtonStories.StandardAttributes} />
