import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as MenuStories from './menu.stories'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'

<Meta of={MenuStories} />

# Menu

## Styles Only Usage

The Menu component is a dropdown menu that can contain one or more Menu Groups. Each Menu Group can have a title and a list of Menu Item Buttons. Menu typically used with Menu Button, it provide a panel that can contain list of buttons, links, .etc

<Canvas of={MenuStories.StylesOnlyUsage} />

<RenderHtmlMarkup of={MenuStories.StylesOnlyUsage} />

## React Shorthand

<Controls />

In the React Shorthand, you can compose your menu by applying props as appropriate following the `Menu` interface

<Canvas of={MenuStories.ReactShorthand} />

<RenderHtmlMarkup of={MenuStories.ReactShorthand} />
