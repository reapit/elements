import { Meta, Canvas, Controls } from '@storybook/blocks'
import { RenderHtmlMarkup } from '../../storybook/render-html-markup'
import * as NavItemStories from './nav-item.stories'

<Meta of={NavItemStories} />

# Nav Item

A versatile component designed to render navigation icon items within an `AppBar`

`NavItem` can be used as an `anchor` or `button` as you need.

<Controls />

## Default State

The default appearance of the `NavItem`

<Canvas of={NavItemStories.Default} />

## Active State

Demonstrates how to use the `NavItem` using a standard `HTMLAnchorElement`

<Canvas of={NavItemStories.ActiveState} />

<RenderHtmlMarkup of={NavItemStories.ActiveState} />

## React Usage

in React version, `NavItem` will automatically use HTML `a` when there is `href` props provided.

<Canvas of={NavItemStories.ReactUsageAnchor} />

<RenderHtmlMarkup of={NavItemStories.ReactUsageAnchor} />

<Canvas of={NavItemStories.ReactUsageButton} />
