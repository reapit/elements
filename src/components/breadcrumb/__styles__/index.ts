import { styled } from '@linaria/react'

export const ElBreadCrumbItem = styled.a`
  font-weight: var(--breadcrumb-item-font-weight);
  border-radius: var(--breadcrumb-item-border-radius);
  font-size: var(--breadcrumb-item-font-size);
  color: var(--breadcrumb-item-font-color);
  margin-right: var(--breadcrumb-item-spacing);
`

export const ElBreadCrumbContainer = styled.div`
  display: flex;
  width: 100%;
`
