{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // We do not need to spend time checking types when we're emitting declaration types
    "noCheck": true
  },
  "include": ["src"],
  "exclude": ["*.test.*", "*.stories.*", ".yarn", "archive", "dist", "node_modules", "public"]
}
